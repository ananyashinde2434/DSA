#include<iostream>
using namespace std;

void printGfg(int N) {
    if (N <= 0) {  // Base case to stop recursion when N is 0 or less
        return;
    } else {
      
    }
     // Recursive call with N-1
}

int main() {
    int n;
    cin >> n;
    printGfg(n); 
    cout << "Gfg "; // Call the function with the user input
    return 0;
}
